while(detect(
             FORWARD(),OBJECT.NOTHING)){
             move(FORWARD()); 
             }
mo
if(detect(
          FORWARD(),OBJECT.WALL)){
           draw();
}
else{
     move(LEFT());
    };