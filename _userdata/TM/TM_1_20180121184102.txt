if(!detect(LEFT(),OBJECT.WALL)){
    move(LEFT());
}
while(!detect(FORWARD(),OBJECT.WALL)){
    move(FORWARD());
}
if(!detect(RIGHT(),OBJECT.WALL)){
    move(RIGHT());
}
while(!detect(FORWARD(),OBJECT.WALL)){
    move(FORWARD());
}